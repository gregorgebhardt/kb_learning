---
# DEFAULT
name: "DEFAULT"
repetitions: 20 # 20
iterations: 20
path: "data/learn_quad_pushing_multiprocessing"
plotting: true
gui: false

params:
  sampling:
    num_episodes: 100 # 100
    num_steps_per_episode: 125 # 125
    num_SARS_samples: 10000 # 10000
    num_workers: 8
    multiprocessing: true
    num_kilobots: 15
  kernel:
    num_processes: 1
  lstd:
    num_features: 1000 # 1000
  gp:
    num_sparse_states: 500 # 1000
---
name: "learn_5_w_factors_kilobot_kernel"
list:
  sampling:
    w_factor: [.0, .25, .5, .75, 1.]
---
name: "learn_5_w_factors_mean_kernel"
params:
  kernel:
    type: 'mean'
list:
  sampling:
    w_factor: [.0, .25, .5, .75, 1.]
---
name: "learn_5_w_factors_meancov_kernel"
params:
  kernel:
    type: 'mean-cov'
list:
  sampling:
    w_factor: [.0, .25, .5, .75, 1.]
